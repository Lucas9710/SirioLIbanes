<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.com.sirioibanes">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".activities.SplitterActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.RegisterActivity" />
        <activity android:name=".activities.HomeActivity" />
        <activity android:name=".activities.ScannerActivity" />
        <activity android:name=".activities.EventActivity" >
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="www.eventossiriolibanes.com"
                    android:pathPrefix="/app/index.html"
                    android:scheme="http" />

                <data
                    android:host="www.eventossiriolibanes.com"
                    android:pathPrefix="/app/index.html"
                    android:scheme="https" />

                <data
                    android:host="l.ead.me"
                    android:path="/barGy7"
                    android:scheme="http://" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.SocialNetworksActivity" />
        <activity android:name=".activities.MusicActivity" />


    </application>

</manifest>